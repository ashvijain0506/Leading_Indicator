# Virtual environment
.venv/

# Raw and derived data — the OSHA CSV is 80 MB and is never committed
data/

# DuckDB database files
*.duckdb
*.duckdb.wal

# Jupyter
.ipynb_checkpoints/

# macOS
.DS_Store

# Orchestration / kickoff prompts — scaffolding, not part of the deliverable.
# PRD.md, CLAUDE.md and PROJECT_BRIEF.md stay tracked.
KICKOFF_PROMPT.md
*KICKOFF*.md
*_PROMPT.md

# Row-level Excel export: 33.69 MB, regenerated by the notebook in seconds.
# output/pivot_source.csv (0.25 MB) is the recommended pivot input and IS tracked.
output/clean_300a.csv
